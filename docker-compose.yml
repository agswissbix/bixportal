services:
  bixportal:
    container_name: bixportal
    image: ghcr.io/<tuo-utente>/bixportal-${CLIENT_ID}:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
    env_file:
      - .env.production